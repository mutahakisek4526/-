<UserControl x:Class="AacV1.Views.KanaBoardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:views="clr-namespace:AacV1.Views">
    <UserControl.Resources>
        <views:FocusMatchToFontWeightConverter x:Key="FocusFontWeightConverter" />
    </UserControl.Resources>
    <StackPanel Margin="24" VerticalAlignment="Center">
        <TextBlock Text="Kana Board" FontSize="24" Margin="0,0,0,16" />
        <TextBox Text="{Binding Text, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,12" />
        <ItemsControl ItemsSource="{Binding Items}" Margin="0,0,0,12">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel />
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Button Content="{Binding}" Padding="8,4" Margin="4"
                            views:Dwell.IsEnabled="True"
                            views:Dwell.Key="{Binding}"
                            views:Dwell.OnCommittedCommand="{Binding DataContext.CommitItemCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}">
                        <Button.FontWeight>
                            <MultiBinding Converter="{StaticResource FocusFontWeightConverter}">
                                <Binding Path="DataContext.FocusedItem" RelativeSource="{RelativeSource AncestorType=ItemsControl}" />
                                <Binding />
                            </MultiBinding>
                        </Button.FontWeight>
                    </Button>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
        <StackPanel Orientation="Horizontal" Margin="0,0,0,12">
            <Button Content="Speak" Command="{Binding SpeakCommand}" Margin="0,0,8,0" />
            <Button Content="Stop" Command="{Binding StopCommand}" Margin="0,0,8,0" />
            <Button Content="Undo" Command="{Binding UndoCommand}" />
        </StackPanel>
        <Button Content="Home" Command="{Binding GoHomeCommand}" />
    </StackPanel>
</UserControl>
